version: "3.9"

services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      JWT_SECRET: valtozd_meg_eles_kornyezetben_!
      NODE_ENV: production
      DB_PATH: /data/mozifoglalo.db
    volumes:
      - db_data:/data
    restart: unless-stopped

volumes:
  db_data: